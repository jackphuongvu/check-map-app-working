import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  MapViewContainer : {
    flex : 1
  },
  MarkerImage : {
    width : 30,
    height : 30
  }
});

export default styles;